# 共通ベース設定（すべての実験で共通）
base_config = {
    "epochs": 50,
    "lr": 1e-4,
    "lambda_out": 1.0,     # 蒸留損失 (KL)
    "lambda_feat": 1.0,    # 特徴蒸留（未使用なら0でもOK）
}

# (i) 非蒸留
config_no_kd = {
    **base_config,
    "mode": "no-kd",
    "lambda_out": 0.0,   # 蒸留なし
    "lambda_feat": 0.0,
    "lambda_E": 0.0,
    "lambda_cons": 0.0,
}

# (ii) 固定温度蒸留 (Fixed-T)
config_fixedT = {
    **base_config,
    "mode": "fixed-T",
    "T_fixed": 2.0,      # 固定温度
    "lambda_E": 0.0,
    "lambda_cons": 0.0,
}

# (iii) DTD（時刻依存温度蒸留）
config_dtd = {
    **base_config,
    "mode": "DTD",
    "lambda_E": 0.0,
    "lambda_cons": 0.0,
}

# (iv) E-DTD（省エネ考慮, 提案）
config_e_dtd = {
    **base_config,
    "mode": "E-DTD",
    "lambda_E": 1e-3,     # 省エネ正則化を有効化
    "lambda_cons": 0.0,
}

# (v) E-DTD + Consistency（提案）
config_e_dtd_cons = {
    **base_config,
    "mode": "E-DTD+CONS",
    "lambda_E": 1e-3,
    "lambda_cons": 0.5,   # Consistency項を追加
}
